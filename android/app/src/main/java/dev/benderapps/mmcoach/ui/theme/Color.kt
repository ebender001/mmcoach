package dev.benderapps.mmcoach.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * University of Michigan brand palette (maize and blue). Mirrors iOS
 * `App/Theme.swift`. Brand colors are fixed values (not light/dark
 * adaptive) per University of Michigan visual identity guidelines.
 */
val MichiganBlue = Color(0xFF00274C)
val Maize = Color(0xFFFFCB05)

// Michigan Blue, adapted for legible text/icon use in dark mode (the raw
// navy is too close to a near-black dark background otherwise).
val MichiganBlueTextLight = Color(0xFF00274C)
val MichiganBlueTextDark = Color(0xFF70A8E0)

val SlateTextLight = Color(0xFF5A636E)
val SlateTextDark = Color(0xFF9EA7B0)

val MutedTealLight = Color(0xFF3A7D78)
val MutedTealDark = Color(0xFF5CA8A3)

val WarmBackgroundLight = Color(0xFFFAF8F4)
